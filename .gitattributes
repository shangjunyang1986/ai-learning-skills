# Normalize line endings. Keep shell scripts LF (CRLF breaks bash),
# PowerShell CRLF, and treat images as binary.
* text=auto eol=lf
*.sh    text eol=lf
*.ps1   text eol=crlf
*.png   binary
*.skill binary
